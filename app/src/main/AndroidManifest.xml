<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ludovic.crespeau.mangareader">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".App"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.allmanga.AllMangaActivity"/>
        <activity android:name=".view.manga.MangaActivity"/>
        <activity android:name=".view.favoris.FavorisActivity"/>
        <activity android:name=".view.chapters.ChapterActivity"/>
        <activity android:name=".view.search.SearchActivity"/>
        <activity android:name=".view.resultActivity.ResultatActivity"/>
        <activity android:name=".view.page.PageActivity"/>
    </application>

</manifest>